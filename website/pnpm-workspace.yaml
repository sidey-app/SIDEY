allowBuilds:
  '@parcel/watcher': true
  esbuild: true

# CI performs an explicit frozen install before scripts run. Keeping this off
# prevents pnpm from replacing node_modules implicitly during local commands.
verifyDepsBeforeRun: false
