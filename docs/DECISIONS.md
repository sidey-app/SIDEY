# SIDEY 결정 기록

- 최종 갱신: 2026-08-31
- 관련 문서: [제품 기획서](PRODUCT_SPEC.md)
- 현재 구현 브랜치: `feature/pixel-world`

이 문서는 SIDEY의 제품·기술 결정을 추적한다. 구현이나 다른 문서와 충돌하면 여기서 `확정`으로 기록한 결정을 우선한다.

## 확정된 결정

| 항목 | 결정 | 이유 |
| --- | --- | --- |
| 제품명 | `SIDEY` | 공식 제품명이다. `같이온`·`같이ON`은 제품 문구, 코드, 식별자에 사용하지 않는다. |
| 제품 형태 | 최대 20명의 실제 친구가 2D 픽셀 동물로 화면 가장자리에서 함께 움직이는 초대 전용 데스크톱 ambient messenger | 고정형 채팅 목록보다 친구의 존재와 짧은 대화가 자연스럽게 보이는 경험에 집중한다. AI 동료나 가상 반려동물 제품은 아니다. |
| 첫 구현 범위 | macOS 네이티브와 `pixel_hamster` 한 종 | 한 캐릭터로 20명 렌더링, 혼잡, 상태, 메시지 UX를 먼저 검증한다. 다른 동물과 Windows는 별도 결정 전까지 만들지 않는다. |
| 플랫폼 순서 | macOS 26 이상 Apple Silicon 우선, Windows 후속 | 장시간 오버레이 안정성과 성능을 한 플랫폼에서 먼저 검증한다. |
| macOS 클라이언트 | SwiftUI + AppKit + SpriteKit | 일반 창은 SwiftUI/AppKit, 창 정책은 AppKit, 픽셀 월드는 SpriteKit이 담당한다. |
| 백엔드 | Supabase Auth, Postgres, Realtime Presence·Broadcast | 기존 인증·방·메시지 계약을 유지하면서 클라이언트 표현만 전환한다. |
| 그룹 인원 | 방당 최대 20명, 서버에서 강제 | 20번째 가입은 허용하고 21번째는 `member_limit_reached`로 거부한다. 클라이언트 검증만으로 대체하지 않는다. |
| 사용자당 그룹 | 최대 5개, 한 번에 활성 그룹 하나만 표시 | 그룹 수 제한은 기존 서버 계약을 유지하고 화면에는 선택한 월드 하나만 노출한다. |
| 그룹 공개 범위 | 초대 전용 비공개 그룹 | 공개 검색·추천·발견 기능은 범위 밖이다. |
| 표시 이름과 캐릭터 | 닉네임과 캐릭터 선택 중복 허용, UUID로 식별 | 표시 속성의 중복이 멤버 식별이나 권한 판단에 영향을 주지 않게 한다. |
| 활동 영역 | 선택한 모니터의 visible frame에 붙는 기본 깊이 180pt의 투명 띠 | 메뉴바·Dock을 제외한 실제 사용 가능 영역 안에서만 월드가 움직인다. 깊이는 작은 화면에서 visible frame 세로 길이의 1/3 이하로 제한한다. |
| 영역 프리셋 | 가장자리 `하단·좌측·우측·상단` × 길이 `1/3·1/2·전체`의 12개 조합, 짧은 띠는 중앙 정렬, 기본 `하단·전체` | 자유 이동·크기 조절 대신 예측 가능하고 복원 가능한 배치를 사용한다. |
| 방향 | 캐릭터의 발이 선택한 가장자리를 향하며 닉네임·상태 점·말풍선도 같은 각도로 회전 | 하단 0°, 좌우 ±90°, 상단 180°로 월드 자체의 방향을 일관되게 유지한다. |
| 모니터 | 월드와 입력창은 선택한 모니터 한 대에만 표시하며, 제거되면 주 화면으로 복귀하고 설정도 갱신 | 사라진 화면 좌표를 계속 복원하는 오류를 막는다. |
| 멤버 표시 | 내 캐릭터를 포함한 활성 그룹 전체를 기본 표시 | 오프라인 멤버는 수면·빨간 점으로 남기며 `오프라인 멤버 표시` 설정을 끄면 숨긴다. |
| 상태 표현 | 온라인=`산책/idle + 초록`, 자리 비움=`수면 + 주황`, 오프라인=`수면 + 빨강`, 재연결=`정지 + 회색` | 서버 상태와 화면 표현을 명확하게 분리한다. |
| 타이핑 표현 | 현재 행동을 유지하고 내용 없는 `…` 말풍선만 표시 | 타이핑을 별도 캐릭터 모션으로 만들지 않는다. 실제 메시지 말풍선이 있으면 메시지가 우선한다. |
| 이동 | 클라이언트 로컬 랜덤 목적지 산책, 간헐적 idle, 부드러운 상호 회피 | 위치는 서버나 Realtime으로 전송하지 않는다. 공간이 부족하면 겹침을 허용하며 영역·설정을 자동 변경하지 않는다. |
| 안정적 배치 | 설치별 로컬 seed와 room/user UUID를 조합해 초기 위치를 결정하고 UUID diff로 노드를 갱신 | 앱 재실행 시 배치를 안정화하고 Presence·스냅샷 갱신 때 기존 위치가 초기화되지 않게 한다. |
| 말풍선 | 발신자별 최신 1개, 전체 최대 4개, 기본 10초, 다섯 번째 발신자 등장 시 가장 오래된 말풍선 제거 | 20명 환경에서 화면 과밀을 제한한다. 실제 메시지 말풍선이 떠 있는 발신자만 이동을 멈춘다. |
| 메시지 전송 | UUID 기반 낙관적 표시 후 Postgres 저장과 조정, 실패 시 말풍선·낙관 기록 제거 및 입력 원문 복구 | 즉시 반응과 서버 원본성을 동시에 유지한다. ledger는 `senderID`를 보존한다. |
| 조용히 모드 | 메시지 본문 말풍선은 숨기고 미확인 수는 유지하며, 내용 없는 타이핑 `…`은 표시 | 화면 공유 중 본문 노출을 막되 상대가 입력 중이라는 신호는 유지한다. |
| 입력창 | 400×56 채팅 입력창과 전송 버튼만 선택 모니터 하단 중앙에 고정 | 오버레이 조작 UI를 최소화한다. 하단 월드는 입력창 주변 440×76을 회피 구역으로 사용한다. |
| 제거하는 오버레이 조작 | 잠금, 직접 이동, 크기 조절, 오버레이의 기록·설정 버튼 제거 | 프리셋 기반 영역 모델과 충돌하는 과거 3D 조작 계약을 남기지 않는다. |
| 최근 기록 | 메뉴바에서 여는 일반 macOS 창 | 클릭 통과 오버레이와 키보드·스크롤 입력을 분리한다. |
| 메뉴바 | 오버레이 표시, 메시지 작성, 활성 그룹·미확인 수, 조용히 모드, 최근 기록, 그룹 설정, 로그인 실행, 설정, 종료만 제공 | 제거된 잠금·이동·크기 기능이 우회 경로로 남지 않게 한다. |
| 렌더링 소유권 | `PixelWorld`가 캐릭터 노드, 이동, 닉네임, 상태 점, 말풍선을 단독 소유 | `AppModel`은 방·Presence·메시지·설정 상태만 소유해 서버 상태와 화면 렌더링을 분리한다. |
| 환경설정 계약 | `OverlayEdge`, `OverlaySpan`, `OverlayRegionPreference(edge, span, screenIdentifier)`를 Codable로 저장 | 스키마를 올리고 과거 frame·lock·scale 값은 읽기 호환만 제공한 뒤 `하단·전체` 영역으로 이전한다. |
| 첫 에셋 | `pixel_hamster`, 24×24 기준, 2배 정수 확대 약 48pt, nearest-neighbor | 크림·골든 햄스터와 작은 페리윙클 포인트를 쓰며 idle 2, walk 4, sleep 2 프레임만 포함한다. |
| 과거 캐릭터 호환 | 클라이언트에서 `minty_pup`을 `pixel_hamster`로 표시하는 alias 제공 | DB 프로필을 즉시 일괄 수정하지 않아도 새 런타임이 안전하게 표시할 수 있다. |
| 메시지·Realtime 스키마 | Postgres 메시지가 원본, Presence는 연결·활동 상태, Broadcast는 타이핑 같은 일시 이벤트 | 픽셀 월드 위치나 애니메이션을 네트워크 계약에 넣지 않는다. |
| 데이터 보안 | Supabase RLS 적용, 방 멤버십·20명·사용자당 5개 방 제한을 서버에서 강제, 초대 코드는 해시만 저장 | 클라이언트 우회와 초대 코드 유출 피해를 줄인다. |
| 개인정보 | 전역 활동은 마지막 시스템 입력 후 경과 시간과 화면 잠금만 사용. 화면 내용, 활성 앱 목록, 다른 앱의 키, 마우스 좌표, 파일, 마이크, 카메라는 수집하지 않음 | 오버레이 편의를 이유로 불필요한 민감 정보를 수집하지 않는다. |
| 보안 표현 | 설계·구현·검증 전에는 E2EE라고 주장하지 않음 | 전송 암호화와 RLS는 종단간 암호화가 아니다. |
| 창 보장 범위 | 보안 화면, DRM 앱, 권한이 높은 앱, 모든 독점 전체화면 게임 위 표시를 보장하지 않음 | OS 보안 정책을 제품 약속으로 우회할 수 없다. |
| 브랜치 보존 | 기존 3D 앱은 `archive/native-3d-alpha`, 새 구현은 `feature/pixel-world` | 비교·롤백 가능한 기준을 보존하고 검증 전 `main`을 변경하지 않는다. |
| 서버 전환 | 방 정원 migration은 로컬 또는 별도 staging에서 먼저 검증하고 운영에는 아직 적용하지 않음 | 클라이언트·서버 전환 순서가 어긋나는 위험을 줄인다. |
| 승격 조건 | 아래 인수 기준을 모두 통과한 뒤 운영 migration과 `main` 승격을 별도 수행 | feature 브랜치 구현 완료가 곧 운영 출시를 뜻하지 않는다. |

## 현재 검증 기준

- 4개 가장자리 × 3개 길이의 영역, 중앙 정렬, 회전, visible frame, 모니터 제거 fallback이 단위 테스트를 통과해야 한다.
- 20개 노드가 영역 밖이나 NaN 상태로 빠지지 않고 입력창·서로를 회피하며, 혼잡 시 안전하게 겹칠 수 있어야 한다.
- 상태 전환, 오프라인 숨김, 방 전환 UUID diff, 말풍선 교체·eviction·만료·이동 정지·재개를 검증한다.
- 월드는 항상 위·클릭 통과, 입력창만 키보드 포커스 가능, 최근 기록은 일반 창이어야 한다.
- 20번째 가입 성공, 21번째 거부, 여섯 번째 방 거부, RLS, 중복 닉네임·캐릭터 허용을 SQL 테스트한다.
- 20명 30분 실행에서 p95 frame time 40ms 이하, 100ms 이상 main-thread hang 없음, 지속 RSS 증가 없음, 숨긴 월드의 SpriteKit 정지를 확인한다.

## 아직 결정하지 않은 항목

| ID | 항목 | 현재 권장안 | 결정 시점 |
| --- | --- | --- | --- |
| D-006 | 자리 비움 전환 시간 | 5분 유지 | 실제 장시간 테스트 후 |
| D-007 | 메시지 말풍선 시간 | 10초 유지 | 20명 비공개 UX 테스트 후 |
| D-010 | Windows 최소 지원 버전과 네이티브 브리지 | 미정 | macOS vertical slice 승인 후 |
| D-012 | E2EE 도입 여부와 프로토콜 | MVP 이후 별도 설계 | 보안 로드맵 수립 시 |
| D-015 | 추가 픽셀 동물 종류와 제작 순서 | 아직 만들지 않음 | 햄스터 vertical slice와 20명 성능 승인 후 |
| D-016 | 정식 배포·자동 업데이트 방식 | alpha ZIP 이후 결정 | 서명·공증 준비 시 |
