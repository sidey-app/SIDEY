# Local product context for coding assistants; do not commit.
/AGENTS.md
/docs/

# Godot generated files.
/.godot/
/build/

# Private licensed character binaries, extracted textures, and local review output.
/assets/characters/
/artifacts/motion-review/

# Locally compiled native bridge binaries.
/native/macos/bin/
/native/macos/.generated/
/native/macos/src/*.os
/native/macos/.sconsign.dblite

# macOS metadata.
.DS_Store

# Local backend endpoints and publishable keys. Secret/service-role keys never belong here.
.env
.env.*
!.env.example
