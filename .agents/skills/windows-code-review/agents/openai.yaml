interface:
  display_name: "Windows Code Review"
  short_description: "Review native Windows changes for real defects"
  default_prompt: "Use $windows-code-review to review this SIDEY Windows diff and report actionable findings."

policy:
  allow_implicit_invocation: true
