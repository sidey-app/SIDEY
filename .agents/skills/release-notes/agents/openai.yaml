interface:
  display_name: "Release Notes"
  short_description: "Draft attributed platform release notes"
  default_prompt: "Use $release-notes to draft the canonical SIDEY release note from the exact shipped range with verified PR or commit attribution."

policy:
  allow_implicit_invocation: true
