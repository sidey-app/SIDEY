<UserControl
    x:Class="Sidey.App.Controls.SelectionProgress"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:i18n="using:Sidey.App.Localization"
    AutomationProperties.Name="{Binding Value, Source={i18n:I18n Key=profile.applying}, Mode=OneWay}">
  <Border x:Name="Surface" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="4" Padding="3" Visibility="Collapsed" IsHitTestVisible="False">
    <StackPanel Orientation="Horizontal" Spacing="3">
      <ProgressRing x:Name="Ring" Width="18" Height="18" />
      <TextBlock x:Name="Label" Text="{Binding Value, Source={i18n:I18n Key=profile.applying}, Mode=OneWay}" FontSize="11" VerticalAlignment="Center" />
    </StackPanel>
  </Border>
</UserControl>
