name: Deploy website (legacy entrypoint)

on:
  workflow_dispatch:
  workflow_call:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    name: Website deployment
    uses: ./.github/workflows/deploy-website.yml
